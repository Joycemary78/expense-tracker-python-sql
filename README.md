# expense-tracker-python-sql
A console-based expense tracker built with Python and SQLite. Features include adding, viewing, and deleting expenses, calculating total spending, category-wise spending breakdown using SQL GROUP BY, and exception handling for invalid inputs.
